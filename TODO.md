# Hafen Console Client — TODO

> Базируется на Vanilla-клиенте Haven and Hearth.  
> Цель: консольный/экшен опыт с геймпадом, без потери совместимости с мышью.

---

## Архитектура изменений

```
src/haven/
├── gamepad/
│   ├── GamepadManager.java      # Поллинг геймпада (jinput / SDL2)
│   ├── GamepadState.java        # Снимок состояния за тик
│   └── GamepadConfig.java       # Маппинг кнопок, деадзоны
├── DirectMovement.java          # Генератор кликов по вектору стика
├── SmartTarget.java             # Умный таргет в конусе
├── RadialPicker.java            # Радиальное окно выбора объектов
├── CameraController.java        # Расширение FollowCam / FreeCam
└── ConsoleHUD.java              # Минимальный HUD для консольного режима
```

---

## Блок 1 — Геймпад: базовый слой

### 1.1 Интеграция библиотеки
- [x] Выбрать и добавить зависимость в `build.xml` / `lib/`
  - Вариант A: **jinput** (уже встречается в HnH-моддинг-сообществе, чистый Java) ✓ выбран
  - Вариант B: **SDL2 через JNA** (лучше поддержка геймпадов, rumble)
  - **Рекомендация:** jinput для старта, SDL2 при необходимости
- [x] `GamepadManager` — поллинг в отдельном треде, публикует `GamepadState` без блокировок (volatile snapshot)
- [x] `GamepadConfig` — JSON/properties-файл с маппингом: кнопки, деадзоны, инверсия осей

### 1.2 Подключение к UI-тику
- [x] В `GameUI.tick()` читать `GamepadState` и диспетчеризировать:
  - стики → `DirectMovement` и `CameraController` ✓
  - кнопки → синтетические KeyEvent / MouseEvent или прямые вызовы ✓

---

## Блок 2 — Direct Movement (левый стик)

### Как работает движение в Vanilla
Клиент шлёт `wdgmsg("click", screenCoord, worldCoord, button, mods, ...)` в `MapView`.  
Сервер отвечает `OD_LINBEG` / `OD_LINSTEP` дельтами → `LinMove`. Персонаж движется к целевой точке, может выйти за неё максимум на `MAXOVER = 0.5` сек (см. `LinMove.java:34`).

### Расчёт дистанции клика

Скорость персонажа в HnH **фиксирована** на уровне сервера — клик в любую точку даёт одинаковую скорость бега.  
Поэтому дистанция клика — константа, подобранная экспериментально.

| Параметр | Значение |
|---|---|
| Интервал кликов | 150–200 мс |
| Целевая дистанция | **~2 тайла** вперёд по вектору стика |
| Формат координат | `posres = 2^-10 × 11` на ось (`OCache.java:59`) |

**Правило остановки:** при отпускании стика персонаж дойдёт до последней точки и остановится. При 2 тайла — чисто. При слишком малой (< 0.5 тайла) — сервер не начнёт новый LINBEG → рывки. При слишком большой (> 3–4 тайла) — персонаж «уплывает» после отпускания.

### Задачи
- [x] `DirectMovement` — класс, получает вектор `(dx, dy)` от стика каждые 150 мс
  - [x] Нормировать вектор, учесть угол камеры (`MapView.camera.angle()`)
  - [x] Вычислить целевую экранную точку: `player_screen_pos + dir_rotated * click_dist_px`
  - [x] Сконвертировать в мировые координаты через `MapView.screenxf()` / существующий ray-cast
  - [x] Отправить через `MapView.wdgmsg("click", ...)`
- [x] Деадзона стика: `|stick| < 0.15` → не отправлять клик (не сбрасывать движение)
- [x] При `|stick| == 0` (отпускание) — прекратить отправку; персонаж сам остановится
- [x] Tune-параметры вынести в `GamepadConfig` (дистанция, интервал)
- [?] **ТЕСТ:** Убедиться, что synthetic-клики не конфликтуют с реальными мышиными (флаг `from_gamepad`)

---

## Блок 3 — Камера

### 3.1 Снятие углового блока (приоритет)
`FollowCam` в `MapView.java:195–203` блокирует свободный наклон камеры двумя ограничениями:
- `maxang = PI/2 - 0.1` — нельзя смотреть строго сверху вниз
- `if(dist(telev) < mindist) telev = fe` — при малых углах отклоняет изменение elevation,  
  фактически блокируя вид «с горизонта»

- [?] **ТЕСТ:** Убрать или вынести в конфиг `maxang` → разрешить полный диапазон `[epsilon, PI/2]` (требует проверки FollowCam)
- [?] **ТЕСТ:** Убрать блок `dist(telev) < mindist` для elevation (оставить только для zoom)
- [?] **ТЕСТ:** Добавить нижний предел elevation ~`PI/16` чтобы камера не уходила под землю
- [x] L2+RS-Y управляет `telev` напрямую без этих блоков (уже существующая интерполяция подхватит)

### 3.2 Управление камерой правым стиком

#### Без L2 — правый стик = мышь
- [x] RS `(rx, ry)` → синтетические `MouseMoveEvent` в `UI` (дельта позиции)
- [x] Чувствительность настраивается отдельно от движения
- [x] Реальная мышь не отключается — оба источники суммируются

#### С зажатым L2 — правый стик = вращение/наклон камеры
- [x] L2 как модификатор: `GamepadState.l2_held`
- [x] RS-X → `camera.tangl += rx * sensitivity` (аналог перетаскивания средней кнопкой)
- [x] RS-Y → `camera.telev += ry * sensitivity`, зажать в `[PI/8, maxang]`
- [x] Использовать существующую анимацию (`FollowCam.tick()` уже интерполирует `angl`/`elev`)
- [?] **ТЕСТ:** При отпускании L2 — плавно отдать RS обратно под мышь (без рывка)

---

## Блок 4 — Умный таргет (R1)

R1 = **умный правый клик** на лучший объект в конусе перед игроком.  
Правый клик в vanilla HnH контекстно-зависим: на лут → подобрать, на станок → открыть меню, на моба → атаковать/флаер.  
Если появляется FlowerMenu от сервера — оно навигируется RS + A.

### Алгоритм поиска в конусе

```
cone_angle = 60° (±30° от вектора взгляда персонажа)
cone_depth  = 5–8 тайлов
```

Вектор взгляда = направление последнего движения или угол камеры, если стоит.

### Режимы таргета

#### Обычный режим

| Приоритет | Категория | Ресурс-путь |
|---|---|---|
| 1 | **Двери / врата** | `gfx/terobjs/gate`, `gfx/terobjs/door` |
| 2 | **Лут / предметы на земле** | `gfx/terobjs/items/` |
| 3 | **Мешки внутри повозки** | bag-объекты рядом с vehicle-gob |
| 4 | **Станки / рабочие столы** | `gfx/terobjs/` (craft-подтипы) |
| 5 | **Контейнеры** | `gfx/terobjs/chest`, `gfx/terobjs/basket` |
| 6 | **Животные / НПС** | `gfx/kritter/`, `gfx/borka/` |
| 7 | **Ресурсы** (деревья, камни) | `gfx/trees/`, `gfx/stones/` |
| — | **Повозка сама по себе** | игнорируется если рядом есть мешки |

#### Боевой режим (отдельный флаг, переключатель L1)

| Приоритет | Категория |
|---|---|
| 1 | Текущий lock-цель (если есть) |
| 2 | Ближайший агрессивный моб |
| 3 | Ближайший нейтральный моб / животное |
| 4 | Игрок (PvP) |

В боевом режиме обычный лут/станки убираются из конуса — только цели для атаки.  
Визуальный индикатор: цвет курсора / иконка в HUD меняется.

### Механика повозки (матрёшка)
Повозка — самостоятельный Gob. При размещении предметов в ней на карте появляются  
**отдельные Gob-мешки** в пределах 1–2 тайлов от позиции повозки.  
Кликая повозку — цепляешь её. Кликая мешок — забираешь содержимое.

Поведение SmartTarget:
- Мешки рядом с vehicle-gob → приоритет 3 (выше самой повозки)
- Если несколько мешков → `RadialPicker` со списком мешков
- Сама повозка попадает в таргет только если мешков нет рядом

### Задачи
- [x] `SmartTarget` — сканирует `glob.oc`, фильтрует объекты в конусе
  - [x] Ресурс-имя: `Gob.getattr(ResDrawable.class).res.get().name`
  - [x] Угол от игрока до объекта vs. вектор взгляда
- [x] Определение «вектора взгляда»:
  - [x] Если стик двигался < 1 сек назад — последнее направление движения (хранить постоянно)
  - [x] Иначе — XY-проекция вектора «от камеры к персонажу»
- [x] Подсветка таргета: голубое кольцо вокруг выбранного Gob (рисуется в `GamepadDispatcher.drawOverlay()`)
- [x] Флаг боевого режима: автодетект из `Fightview.lsrel` — включается когда сервер открывает бой (НПС атакует или игрок инициирует), L1 больше не нужен
- [x] Визуальная индикация боевого режима в HUD: бейдж «⚔ COMBAT» в верхнем левом углу

#### R1 одиночное нажатие — умный правый клик
- [x] Найти лучший Gob в конусе по таблице приоритетов (с учётом режима)
- [x] Один объект → правый клик: `wdgmsg("click", ..., 3, ...)`
- [x] Несколько в одном приоритетном классе → `RadialPicker`
- [?] **ТЕСТ:** Если появился FlowerMenu от сервера → он навигируется RS + A (не блокируем vanilla-флаер)

#### R1 удержание — радиальное окно ближайших объектов
- [x] Порог удержания: 300–400 мс (350 мс)
- [x] Собрать до 8 ближайших объектов всех приоритетов
- [x] Открыть `RadialPicker`
- [?] **ТЕСТ:** RS-угол → сектор, отпускание R1 → правый клик по выбранному

---

## Блок 5 — RadialPicker (радиальное окно)

### Требования к UI
- Максимально близко к стилю vanilla FlowerMenu (`FlowerMenu.java`)
  - Те же текстуры: `Window.wbox`, `Window.bg`, `pbg`
  - Те же цвета и шрифты: `ptf`, `ptc`
  - Та же анимация раскрытия (вынести `Opening` в общий базовый класс)
- Отличия: поддержка навигации стиком (текущий сектор подсвечивается)

### Задачи
- [x] `RadialPicker extends Widget`:
  - [x] Принимает `List<GobEntry>` (gob + иконка + label)
  - [x] Раскладывает по кругу (аналог `FlowerMenu.place()`)
  - [x] Слушает `GamepadState.rs_angle` для выбора сектора
  - [x] При `R1 release` или `Cross/A` — вызывает коллбэк с выбранным Gob
- [x] Иконки объектов: `Gob.getattr(ResDrawable.class)` → превью спрайта или иконка ресурса
- [?] **ТЕСТ:** Решение матрёшки для повозок — уже описано в Блоке 4

---

## Блок 6 — Маппинг кнопок

### Таблица маппинга

| Кнопка | Без L2 | С зажатым L2 |
|---|---|---|
| **A (Cross)** | `1` (hotbar slot) | `5` |
| **B (Circle)** | `2` | `6` |
| **X (Square)** | `3` | `7` |
| **Y (Triangle)** | `4` | `8` |
| **R1** | Умный таргет (tap) / RadialPicker (hold) | — |
| **R2** | ПКМ / контекстное действие | — |
| **L1** | _(свободна — можно забиндить на блок/уклонение в бою)_ | — |
| **L2** | Модификатор (hold) | — |
| **LS (нажатие)** | Sprint / Walk toggle | — |
| **RS (нажатие)** | Сброс камеры на север | — |
| **D-pad ↑↓←→** | Инвентарь открыт → навигация по слотам; закрыт → навигация по иконкам меню (крафт, перки, …) | — |
| **Start / Options** | Главное меню / пауза | — |
| **Select / Share** | Карта (`MapWnd`) | — |
| **RS (ось)** | Движение мыши | Вращение камеры |
| **LS (ось)** | Direct movement | — |

> Все маппинги — в `GamepadConfig`, переопределяются пользователем.

### Задачи
- [x] ABXY → синтетические `KeyDownEvent` / `KeyUpEvent` с кодами `1`–`8`
- [x] L2 как stateful modifier: `GamepadState.l2_held` проверяется в диспетчере
- [x] R2 → синтетический правый клик мыши на текущем таргете или под курсором
- [?] **ТЕСТ:** Предотвратить двойное срабатывание при переходе L2 hold/release

---

## Блок 7 — Разное / Полировка

- [ ] Курсор мыши: при активном движении стиком — скрывать OS-курсор (опционально)
- [x] HUD-индикатор геймпада: зелёный «⌖ GAMEPAD» + красный «⚔ COMBAT» в левом верхнем углу
- [ ] Настройки: вкладка «Gamepad» в существующем `PreferencesWnd`
- [?] **ТЕСТ:** Тест совместимости: убедиться, что мышь + стик работают одновременно без конфликтов

---

## Открытые вопросы

1. **Боевой режим — автоактивация?** Нужно ли включать боевой режим автоматически при агро (моб атакует игрока), или только вручную через L1?
   - **СТАТУС:** Автодетект из `Fightview.lsrel` — бой включается и выключается зеркально серверному состоянию

2. **Вектор взгляда при простое**: Хранить последнее направление движения бессрочно, или после N секунд простоя брать вектор «от камеры к персонажу»?
   - **СТАТУС:** Реализовано как описано, требует тестирования поведения

3. **D-pad и инвентарь**: Как переключать фокус между несколькими открытыми окнами инвентаря — например, сундук + рюкзак одновременно? Нужна ли кнопка для смены фокуса?
   - **СТАТУС:** Базовая поддержка добавлена, требуется уточнение и сложная логика для нескольких окон

4. **Библиотека геймпада**: jinput не обновлялся с 2017. Есть ли предпочтения? SDL2/JNA — лучше поддержка PS/Xbox геймпадов и rumble, но сложнее в сборке.
   - **СТАТУС:** выбран jinput, можно заменить на SDL2 при необходимости

5. **RadialPicker — максимум объектов**: При удержании R1 в густом лесу могут попасть десятки деревьев. Ограничить до 8? Или показывать только топ-N по каждой категории?
   - **СТАТУС:** Ограничено до 8, требует тестирования в густых областях

---

## Порядок реализации (рекомендуемый)

```
[1] Геймпад базовый слой          → GamepadManager, GamepadState
[2] Direct Movement               → DirectMovement (самое заметное, быстрый фидбэк)
[3] Кнопки ABXY + L2/L1-модиф    → диспетчер в GameUI.tick()
[4] Камера RS / L2+RS             → CameraController
[5] Угловой блок камеры           → правка FollowCam (maxang, minelev)
[6] D-pad контекст                → навигация инвентарь / иконки меню
[7] SmartTarget R1 tap            → SmartTarget + оверлей
[8] Боевой режим L1               → CombatMode, смена приоритетов таргета
[9] RadialPicker R1 hold          → RadialPicker UI
[10] Матрёшка / повозки           → bag-gob detection рядом с vehicle
[11] Полировка / настройки        → ConsoleHUD, PreferencesWnd вкладка
```
